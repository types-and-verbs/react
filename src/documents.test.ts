describe.skip('documents', () => {
  test('', (done) => {
    done();
  });
});

// document

// typesandverbs.setup('5ee9e0fe2a30366ccdd1ebcb');

// Auth
// const { user } = await typesandverbs.signup({
// email: 'test@typesandverbs.com',
// password: 'password',
// });
// const { user } = await
// typesandverbs
// .signin({
// email: '',
// password: 'password',
// })
// .then(res => console.log('signed in as: ', res.user.email))
// .catch(err => console.error(err));

// console.log(typesandverbs.authedUser());

// const { id, name, email } = await typesandverbs.auth.user();

// await typesandverbs.auth.resetPassword({
// token,
// password,
// });

// await typesandverbs.auth.resetPasswordRequest({
// token,
// password,
// });

// CRUD
// const endpoint = 'task';

// const tasks = await typesandverbs.findMany(endpoint, {
// // where: {},
// // orderBy: '',
// });
// const task = await typesandverbs.findOne(endpoint, id);

// const newTask = await typesandverbs.create(endpoint, {
// name: 'cheese2',
// });
// console.log(newTask);

// const updatedTask = await typesandverbs.update(endpoint, {
// _id: id,
// name: 'cheese',
// });

// await typesandverbs.remove(endpoint, id);
